# Copy this file to .env and fill in your real values. Never commit .env.

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=geewhy_catalog
DB_PORT=3306

# Password you'll type to access the admin page (/admin)
ADMIN_PASSWORD=change_this_password

# Your business WhatsApp number, digits only, with country code, no + or spaces
WHATSAPP_NUMBER=2349032608002

PORT=3000
